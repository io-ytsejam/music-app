import { GET_TITLE, NEW_TITLE } from "../actions/types";

const initialState = {
  title: {}
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}